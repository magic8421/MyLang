# MyLang 数组/向量重构会话总结

**时间**：2026-07-15 20:33  
**目标**：完成 `T[]` 从 `ObjHeader` 独立为值类型向量的剩余工作，实现 `move_to(ref)` / `copy_to(ref)` 与向量方法。

## 已完成工作

### 1. 调用处 `ref` 参数
- `parser.c` 支持显式 `ref` 实参（生成 `AST_REF_ARG`）。
- `codegen.c` 在 `resolve_type` 和 `codegen_call_arg` 中识别 `AST_REF_ARG`。
- 保留旧行为：标量 `ref` 参数可以不带 `ref` 关键字调用（如 `add_one(a)`）。

### 2. 移除旧数组语义
- 解析器不再接受固定数组 `T[N]`。
- 函数/方法/接口方法中校验：
  - 数组不能按值返回；
  - 数组参数必须是 `ref`。
- `codegen.c` 中数组赋值只能走 `move_to(ref)` / `copy_to(ref)`，不能直接 `=`。

### 3. 向量方法实现
新增运行时辅助函数（`runtime.h` / `runtime.c`）：
- `mylang_array_push`
- `mylang_array_pop`
- `mylang_array_clear`
- `mylang_array_compact`

`codegen.c` 中为数组方法调用生成对应代码：
- `arr.push(value)`
- `arr.pop()`
- `arr.reserve(n)`
- `arr.resize(n)`
- `arr.clear()`
- `arr.compact()`
- `arr.move_to(ref dst)`
- `arr.copy_to(ref dst)`

### 4. 动态弱接口数组修复
- 修复 `c_type_str`：弱接口数组类型现在是 `MyArray`，不是 `WeakIFoo`。
- 修复 `c_array_elem_type_name`：弱接口数组元素类型为 `WeakIFoo`。
- 修复 `runtime.c` 中弱接口数组的元素释放、压入、深拷贝逻辑（元素布局为 `{WeakRef* wr, vtable*}`）。
- 修复弱接口数组元素赋值，改用已生成的 `mylang_weakify_` 辅助函数。
- `new weak IFoo[N]` 现在可被解析。

### 5. 其他修复
- 修复数组成员访问生成（`a.length` 生成 `a.length`，不再错误生成 `a->length`）。
- 修复 `call_needs_guard`，避免把 `ref` 参数和数组误判为需要 guard 的 class/interface 表达式。
- 表达式语句加入错误恢复，防止非法 token 导致解析死循环。

### 6. 测试
新增 4 个测试：
- `array_vector_methods`
- `class_array_vector_methods`
- `array_move_copy`
- `weak_iface_dyn_array`

测试结果：

```text
python test_runner.py          # release + ASan
108/108 passed, 0 failed

python test_runner.py --mode debug
108/108 passed, 0 failed
```

唯一报告的 CRT leak 来自故意构造循环引用的 `circular_ref_leak` 测试，属于预期行为。

### 7. 文档更新
- `AGENTS.md`：数组是独立值类型向量；更新 Runtime 函数列表；移除固定数组描述；更新弱接口数组限制。
- `GRAMMA.md`：只保留 `T[]` 动态数组语法；新增向量方法调用说明；更新内存模型简述。

## 关键设计确认

- `Box[] a;` 会被初始化为空数组，`a.push(new Box)` 会自动扩容，等价于先 `new Box[1]` 再赋值。
- 数组是值类型，不能按值返回或传递，不能直接用 `=` 赋值，必须通过 `ref T[]` 参数和 `move_to(ref)` / `copy_to(ref)` 转移/复制。

## 修改文件

- `src/parser.c`
- `src/codegen.c`
- `src/runtime.h`
- `src/runtime.c`
- `test_runner.py`
- `AGENTS.md`
- `GRAMMA.md`
